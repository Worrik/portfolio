import { extendTheme } from '@chakra-ui/react'
import './index.css'

const theme = extendTheme({
  fonts: {
    heading: 'Poppins, sans-serif',
    body: 'Merienda, cursive',
  },
})

export default theme
